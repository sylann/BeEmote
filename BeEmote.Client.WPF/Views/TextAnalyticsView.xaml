<UserControl x:Class="BeEmote.Client.WPF.TextAnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BeEmote.Client.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="750">

    <Grid Background="Transparent">

        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header title -->
        <TextBlock x:Name="EmotionTitle" Grid.Row="0" Text="Analyze of a text" Style="{StaticResource MainControlHeader}"/>

        <!-- View Main Content -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Top Part: Text input -->
            <Border Grid.Row="0" x:Name="TextInputArea" Style="{StaticResource PaddedWhiteSubControl}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Button Grid.Row="0" x:Name="SendTextButton" Content="Analyze" Style="{StaticResource FlatWhiteButton}" Click="SendTextButton_Click"/>
                    <TextBox Grid.Row="1" x:Name="TextToAnalyseTextBox" Text="Input some text here" Style="{StaticResource BigTextBox}"/>
                </Grid>
            </Border>

            <!-- Bottom part: Result and Stats -->
            <Border Grid.Row="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Analyse result -->
                    <Border Grid.Column="0" x:Name="TextAnalyticsData" Style="{StaticResource PaddedWhiteSubControl}">
                        <StackPanel VerticalAlignment="Center">
                            <ContentControl>
                                <ContentControl.Style>
                                    <Style TargetType="{x:Type ContentControl}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=State}" Value="0">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate>
                                                            <StackPanel>
                                                                <TextBlock Text="No data to analyse yet" Style="{StaticResource CenteredInfoText}"/>
                                                            </StackPanel>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Path=State}" Value="1">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate>
                                                            <StackPanel>
                                                                <TextBlock Text="Analyzing data ..." Style="{StaticResource CenteredInfoText}"/>
                                                            </StackPanel>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Path=State}" Value="2">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate>
                                                            <StackPanel>
                                                                <TextBlock Text="Text Analytics results" Style="{StaticResource SubControlHeader}"/>
                                                                <StackPanel Orientation="Horizontal">
                                                                    <Label Content="Language:" Style="{StaticResource DefaultLabel}"/>
                                                                    <Label x:Name="Languagelabel" Content="{Binding TextAnalyticsResponse.Language.Name}" Style="{StaticResource DefaultLabel}"/>
                                                                    <Label x:Name="LanguageScorelabel" Content="{Binding TextAnalyticsResponse.Language.Score}" Style="{StaticResource DefaultLabel}"/>
                                                                </StackPanel>
                                                                <StackPanel Orientation="Horizontal">
                                                                    <Label Content="Sentiment:" Style="{StaticResource DefaultLabel}"/>
                                                                    <Label x:Name="Sentimentlabel" Content="{Binding TextAnalyticsResponse.Score}" Style="{StaticResource DefaultLabel}"/>
                                                                </StackPanel>
                                                                <StackPanel Orientation="Horizontal">
                                                                    <Label Content="Key Phrases:" Style="{StaticResource DefaultLabel}"/>
                                                                    <Label x:Name="NbKeyPhraseslabel" Content="{Binding TextAnalyticsResponse.NbKeyPhrases}" Style="{StaticResource DefaultLabel}"/>
                                                                </StackPanel>
                                                                <StackPanel Orientation="Horizontal">
                                                                    <Label x:Name="KeyPhraseslabel" Content="{Binding TextAnalyticsResponse.FormattedKeyPhrases}" Style="{StaticResource DefaultLabel}"/>
                                                                </StackPanel>
                                                            </StackPanel>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ContentControl.Style>
                            </ContentControl>
                        </StackPanel>
                    </Border>

                    <!-- Statistics -->
                    <Border Grid.Column="1" Style="{StaticResource PaddedWhiteSubControl}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Calls / day and Score distribution -->
                            <StackPanel Grid.Column="0"  Orientation="Vertical">
                                <TextBlock Text="Statistics" Style="{StaticResource SubControlHeader}" />

                                <StackPanel Orientation="Horizontal">
                                    <Label Content="Calls per day:" Style="{StaticResource DefaultLabel}" />
                                    <Label Content="20" Style="{StaticResource DefaultLabel}" />
                                </StackPanel>

                                <TextBlock Text="Score distribution" Style="{StaticResource SectionHeader}" />

                                <StackPanel Orientation="Horizontal" Style="{StaticResource CompactStack}">
                                    <Label Content="0-30 score:" Style="{StaticResource CompactLabel}" />
                                    <Label Content="25" Style="{StaticResource CompactLabel}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Style="{StaticResource CompactStack}">
                                    <Label Content="0-30 score:" Style="{StaticResource CompactLabel}" />
                                    <Label Content="25" Style="{StaticResource CompactLabel}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Style="{StaticResource CompactStack}">
                                    <Label Content="0-30 score:" Style="{StaticResource CompactLabel}" />
                                    <Label Content="25" Style="{StaticResource CompactLabel}" />
                                </StackPanel>
                            </StackPanel>

                            <!-- Emotion distribution -->
                            <StackPanel Grid.Column="1"  Orientation="Vertical">

                                <TextBlock Text="Language distribution" Style="{StaticResource SectionHeader}" />

                                <StackPanel Orientation="Horizontal" Style="{StaticResource CompactStack}">
                                    <Label Content="Anger:" Style="{StaticResource CompactLabel}" />
                                    <Label Content="25%" Style="{StaticResource CompactLabel}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Style="{StaticResource CompactStack}">
                                    <Label Content="Contempt:" Style="{StaticResource CompactLabel}" />
                                    <Label Content="15%" Style="{StaticResource CompactLabel}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Style="{StaticResource CompactStack}">
                                    <Label Content="Disgust:" Style="{StaticResource CompactLabel}" />
                                    <Label Content="10%" Style="{StaticResource CompactLabel}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Style="{StaticResource CompactStack}">
                                    <Label Content="Fear:" Style="{StaticResource CompactLabel}" />
                                    <Label Content="7%" Style="{StaticResource CompactLabel}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Style="{StaticResource CompactStack}">
                                    <Label Content="Happiness:" Style="{StaticResource CompactLabel}" />
                                    <Label Content="6%" Style="{StaticResource CompactLabel}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Style="{StaticResource CompactStack}">
                                    <Label Content="Neutral:" Style="{StaticResource CompactLabel}" />
                                    <Label Content="5%" Style="{StaticResource CompactLabel}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Style="{StaticResource CompactStack}">
                                    <Label Content="Sadness:" Style="{StaticResource CompactLabel}" />
                                    <Label Content="3%" Style="{StaticResource CompactLabel}" />
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Style="{StaticResource CompactStack}">
                                    <Label Content="Surprise:" Style="{StaticResource CompactLabel}" />
                                    <Label Content="1%" Style="{StaticResource CompactLabel}" />
                                </StackPanel>
                                
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

        </Grid>
    </Grid>
</UserControl>
